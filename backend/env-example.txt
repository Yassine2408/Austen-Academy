# Austen Academy Backend Configuration
# Copy this file to .env and fill in your actual values

# Server Configuration
NODE_ENV=production
PORT=3000
FRONTEND_URL=https://yassine2408.github.io/Austen-Academy

# Database Configuration
MONGODB_URI=mongodb://localhost:27017/austen-academy
# For MongoDB Atlas: mongodb+srv://username:password@cluster.mongodb.net/austen-academy

# Session Security
SESSION_SECRET=your-super-secret-session-key-change-this-in-production

# Admin Authentication
ADMIN_TOKEN=your-secure-admin-token-change-this

# Email Configuration (SMTP with TLS)
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=your-email@gmail.com
SMTP_PASS=your-app-specific-password
CONTACT_EMAIL=info@austenacademymaroc.com

# Security Configuration
BCRYPT_ROUNDS=12
JWT_SECRET=your-jwt-secret-key-for-admin-auth

# Rate Limiting
RATE_LIMIT_WINDOW_MS=900000
RATE_LIMIT_MAX_REQUESTS=5

# File Upload Security (if needed)
MAX_FILE_SIZE=5242880
ALLOWED_FILE_TYPES=image/jpeg,image/png,image/webp,application/pdf

# Monitoring & Logging
LOG_LEVEL=info
ENABLE_ACCESS_LOGS=true

# Production Security
FORCE_HTTPS=true
SECURE_COOKIES=true
TRUST_PROXY=true

# ============================================
# IMPORTANT SECURITY NOTES:
# ============================================
# 1. Change ALL default values before production
# 2. Use strong, unique passwords (minimum 16 characters)
# 3. Enable 2FA on all accounts (email, database, hosting)
# 4. Keep this file secret - add .env to .gitignore
# 5. Use environment-specific configurations
# 6. Regularly rotate secrets and tokens
# 7. Monitor access logs for suspicious activity
# 8. Set up automated backups
# 9. Use HTTPS everywhere (no HTTP in production)
# 10. Keep dependencies updated (npm audit regularly)
